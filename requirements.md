# card_news 요구사항 명세서

- 문서 버전: v1.4
- 최종 수정일: 2026-08-22
- 상태: 확정

---

## 1. 프로젝트 개요

### 1.1 목적

최신 뉴스를 자동으로 수집하여 **카드뉴스 형태의 정적 웹페이지**로 보여주고, 각 카드를 클릭하면 원문 기사로 이동시키는 서비스를 만든다.

### 1.2 핵심 컨셉

- 뉴스 **본문을 재게시하지 않는다.** 제목 / 요약 / 매체명 / 발행시각 / 원문 링크만 다루며, 읽기는 원문 사이트에서 이루어진다.
- 서버가 없다. 수집은 GitHub Actions가 수행하고, 결과물인 `news_link.json`을 저장소에 커밋한다. 페이지는 커밋된 JSON만 읽어 렌더링한다.
- 브라우저가 뉴스 사이트를 직접 호출하지 않으므로 CORS 문제가 발생하지 않는다.

### 1.3 사용자 시나리오

1. 사용자가 GitHub Pages URL로 접속한다.
2. 페이지가 `news_link.json`을 불러와 최신순으로 카드 그리드를 렌더링한다.
3. 사용자가 키워드 칩을 눌러 관심 주제만 필터링하거나, 검색창에 단어를 입력해 좁힌다.
4. 카드를 클릭하면 새 탭에서 원문 기사가 열린다.

---

## 2. 확정된 기술 선택

| 항목 | 결정 | 근거 |
|---|---|---|
| 수집 소스 | 구글뉴스 키워드 RSS | API 키·시크릿 불필요, 키워드 검색 지원, 규격 안정 |
| 수집 방식 | 키워드 기반 | 키워드가 곧 카드 분류(카테고리)가 됨 |
| 수집 스크립트 | Python 3 | Actions 기본 탑재, RSS 파싱 코드가 가장 간결 |
| 프론트엔드 | 빌드 없는 정적 HTML/CSS/JS | 카드 그리드 규모에 프레임워크는 과함, 배포 즉시성 |
| 썸네일 | 텍스트 중심 카드 | 핫링크 차단·깨짐 위험 없음, 로딩 빠름, 저작권 이슈 없음 |
| 디자인 톤 | 샌드 옐로우 웜 팔레트 / 조밀한 밀도 | 사용자 지정. 상세는 FR-W-17·18 |
| 테마 | 시스템 자동 + 수동 3단 전환 | 사용자 지정. 상세는 FR-W-14·15 |
| 갱신 주기 | 매시간 | 속보성 우선 |
| 배포 | GitHub Pages (`main` 브랜치 루트) | 무료, 별도 빌드 워크플로 불필요 |
| 저장소 공개 | `card_news`를 public으로 전환 | private 저장소 Pages는 유료 플랜 전용 |

### 2.1 수집 키워드 (초기값)

`생성형 AI`, `반도체`, `금리`, `주식시장`, `스타트업` — 총 5개.

키워드 목록은 코드에 하드코딩하지 않고 **`config/keywords.json`으로 분리 관리**하여, 파일 수정만으로 추가·삭제·변경이 가능해야 한다.

---

## 3. 기능 요구사항

### 3.1 뉴스 수집 (FR-C)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-C-01 | `config/keywords.json`에 정의된 각 키워드에 대해 구글뉴스 한국어 RSS(`hl=ko&gl=KR&ceid=KR:ko`)를 조회한다. | 필수 |
| FR-C-02 | 각 RSS 항목에서 제목, 원문 링크, 매체명, 발행시각을 추출한다. | 필수 |
| FR-C-03 | 구글뉴스 제목 말미에 붙는 ` - 매체명` 접미사를 제거하여 순수 제목만 저장한다. 같은 매체명이 두 번 반복되는 경우까지 모두 제거한다. | 필수 |
| FR-C-04 | 기사 요약은 저장하지 않는다. 구글뉴스 검색 RSS의 `description`은 기사 요약이 아니라 관련 기사 링크 목록이며, 태그를 제거하면 제목·매체명이 반복되는 무의미한 텍스트가 되기 때문이다. 카드는 제목 중심으로 구성한다. | 필수 |
| FR-C-05 | 발행시각은 KST(Asia/Seoul) 기준 ISO 8601 문자열로 정규화한다. | 필수 |
| FR-C-06 | 기사 링크를 정규화(추적 쿼리 파라미터 제거)한 뒤 SHA-1 해시를 `id`로 부여한다. | 필수 |
| FR-C-07 | `id` 기준으로 중복을 제거한다. 동일 기사가 여러 키워드에 걸리면 하나의 항목으로 병합하고 `keywords` 배열에 모든 키워드를 담는다. | 필수 |
| FR-C-08 | 기존 `news_link.json`을 읽어 신규 항목과 병합한다. 즉 실행 시마다 전체를 덮어쓰지 않고 누적한다. | 필수 |
| FR-C-09 | 발행 후 7일이 지난 항목은 목록에서 제거한다. | 필수 |
| FR-C-10 | 최대 보관 항목 수는 300개로 제한하며, 초과 시 오래된 순으로 버린다. | 필수 |
| FR-C-11 | 특정 키워드의 RSS 조회가 실패해도 나머지 키워드 수집은 계속 진행하고, 실패 사실을 로그로 남긴다. | 필수 |
| FR-C-12 | 모든 키워드 조회가 실패한 경우 기존 `news_link.json`을 훼손하지 않고 종료 코드 1로 실패 처리한다. | 필수 |
| FR-C-13 | 요청에 User-Agent 헤더와 타임아웃(15초)을 설정하고, 키워드 간 1초 간격을 둔다. | 권장 |
| FR-C-14 | **제목 기준 2차 중복제거.** 통신사 기사를 여러 매체가 그대로 싣는 경우 링크가 달라 FR-C-07의 `id` 중복제거를 통과한다. 공백·문장부호·대소문자를 제거한 제목이 같으면 같은 기사로 보고, **가장 먼저 발행된 항목**을 남긴 뒤 나머지 항목의 키워드를 합친다. 발행시각이 같으면 `id`가 작은 쪽을 남겨 실행마다 결과가 흔들리지 않게 한다. | 필수 |

### 3.2 데이터 관리 (FR-D)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-D-01 | 뉴스 데이터는 `data/news_link.json` 단일 파일로 관리한다. | 필수 |
| FR-D-02 | 파일 인코딩은 UTF-8(BOM 없음), 한글은 이스케이프하지 않고(`ensure_ascii=False`) 저장한다. | 필수 |
| FR-D-03 | 들여쓰기 2칸, 키 순서 고정으로 저장하여 커밋 diff가 최소화되도록 한다. | 필수 |
| FR-D-04 | JSON 스키마는 아래 §3.3을 따른다. | 필수 |
| FR-D-05 | 수집 결과가 기존 파일과 완전히 동일하면 파일을 쓰지 않고 커밋하지도 않는다(빈 커밋 방지). **`generated_at`은 기사 목록이 실제로 바뀐 경우에만 갱신한다** — 매번 현재 시각을 써 넣으면 기사가 그대로여도 파일이 달라져 이 요구사항이 무력화된다. | 필수 |

### 3.3 `news_link.json` 스키마 (FR-D-04)

```jsonc
{
  "generated_at": "2026-08-22T14:00:00+09:00",  // 기사 목록이 마지막으로 바뀐 시각 (KST, ISO 8601). FR-D-05 참조
  "keywords": ["생성형 AI", "반도체", "금리", "주식시장", "스타트업"],
  "count": 137,                                  // items 길이
  "items": [
    {
      "id": "9a1f0c...",                         // 정규화된 link의 SHA-1 (40자)
      "title": "삼성전자, HBM4 양산 일정 앞당긴다",
      "link": "https://news.google.com/rss/articles/...",
      "source": "연합뉴스",                       // 매체명. 없으면 ""
      "published_at": "2026-08-22T13:41:00+09:00",
      "keywords": ["반도체"]                      // 이 기사가 매칭된 키워드 목록
    }
  ]
}
```

- `items`는 `published_at` **내림차순**(최신순)으로 정렬되어 저장된다.
- 모든 필드는 필수이며 값이 없을 경우 빈 문자열 또는 빈 배열로 채운다(`null` 금지).

### 3.4 웹 페이지 (FR-W)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-W-01 | 페이지 로드 시 `data/news_link.json`을 fetch하여 카드 그리드를 렌더링한다. | 필수 |
| FR-W-02 | 각 카드는 제목, 매체명, 상대 발행시각("3시간 전"), 키워드 배지를 표시한다. 제목이 카드의 주 시각 요소이며 최대 4줄까지 노출한다. | 필수 |
| FR-W-03 | 카드 전체가 클릭 가능한 링크이며, `target="_blank"` + `rel="noopener noreferrer"`로 새 탭에서 원문을 연다. | 필수 |
| FR-W-04 | 헤더에 키워드 필터 칩을 표시하고, 선택 시 해당 키워드 기사만 보여준다. "전체" 칩으로 해제한다. | 필수 |
| FR-W-05 | 검색 입력창에서 제목·매체명을 대소문자 구분 없이 부분 일치 필터링한다. | 필수 |
| FR-W-06 | 키워드 필터와 검색어는 AND 조건으로 함께 적용된다. | 필수 |
| FR-W-07 | 마지막 갱신 시각(`generated_at`)을 헤더에 사람이 읽는 형식으로 표시한다. | 필수 |
| FR-W-08 | 로딩 중 / 데이터 로드 실패 / 필터 결과 없음 세 가지 상태에 대해 각각 안내 UI를 제공한다. | 필수 |
| FR-W-09 | 반응형 레이아웃: 모바일 1열, 태블릿 2열, 데스크톱 3열 이상 자동 배치. | 필수 |
| FR-W-10 | 라이트/다크 테마를 `prefers-color-scheme`에 따라 자동 적용한다. | 필수 |
| FR-W-11 | 키워드별로 고정된 액센트 색상을 배정하여 배지와 카드 액센트 바에 사용한다. 색상은 샌드 옐로우 톤과 어울리는 어스 컬러 팔레트에서 고른다. | 권장 |
| FR-W-12 | 현재 필터 상태를 URL 쿼리스트링(`?k=반도체&q=HBM`)에 반영하여 링크 공유가 가능하게 한다. | 권장 |
| FR-W-13 | 캐시된 구버전 JSON이 뜨지 않도록 fetch 시 캐시 무효화 쿼리를 붙인다. | 권장 |
| FR-W-14 | 헤더에 테마 전환 버튼을 두어 **시스템 → 라이트 → 다크** 순으로 순환 선택할 수 있게 한다. 선택값은 `localStorage`에 저장되어 재방문 시 유지되며, "시스템"에서는 다시 `prefers-color-scheme`를 따른다. | 필수 |
| FR-W-15 | 테마 적용은 첫 페인트 전에 완료되어야 한다(화면 깜빡임 금지). `<head>`의 인라인 스크립트로 `<html>`에 `data-theme` 속성을 먼저 설정한다. | 필수 |
| FR-W-16 | 현재 필터 조건에 해당하는 기사 건수를 화면에 표시하고, 스크린리더에 변경을 알린다(`aria-live`). | 권장 |
| FR-W-17 | 페이지 전체 색감은 **샌드 옐로우 계열 웜 톤**으로 통일한다. 배경·카드·테두리·본문 텍스트까지 회색이 아닌 따뜻한 계열(황토·모래·크림)을 사용하며, 다크 테마도 차가운 청회색이 아니라 웜 차콜을 기준으로 한다. | 필수 |
| FR-W-18 | 레이아웃 밀도는 **조밀하되 답답하지 않게** 한다. 카드 최소 폭 280px, 카드 간격 14px, 카드 내부 여백 16~18px, 제목 행간 1.5를 기준값으로 하며, 한 화면에 최대한 많은 카드를 담되 텍스트가 서로 붙어 보이지 않아야 한다. | 필수 |

### 3.5 자동화 (FR-A)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-A-01 | GitHub Actions 워크플로가 **매시간**(cron) 수집 스크립트를 실행한다. | 필수 |
| FR-A-02 | `workflow_dispatch`를 지원하여 수동 실행이 가능해야 한다. | 필수 |
| FR-A-03 | 변경이 있을 때만 `data/news_link.json`을 커밋·푸시한다. | 필수 |
| FR-A-04 | 커밋 작성자는 `github-actions[bot]`으로 지정한다. | 필수 |
| FR-A-05 | 워크플로 권한은 `contents: write` 최소 권한으로 한정한다. | 필수 |
| FR-A-06 | 커밋 메시지는 `chore(news): update news_link.json (N items)` 형식으로 통일한다. | 권장 |
| FR-A-07 | 수집 실패 시 워크플로는 실패로 표시되어야 하며, 잘못된 데이터를 커밋해서는 안 된다. | 필수 |
| FR-A-08 | 동시 실행을 방지하기 위해 `concurrency` 그룹을 설정한다. | 권장 |

### 3.6 배포 (FR-P)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-P-01 | GitHub Pages를 `main` 브랜치 루트(`/`)에서 서비스한다. 별도 빌드 워크플로를 두지 않는다. | 필수 |
| FR-P-02 | `index.html`은 저장소 루트에 위치한다. | 필수 |
| FR-P-03 | Jekyll 처리를 방지하기 위해 루트에 `.nojekyll` 파일을 둔다. | 필수 |
| FR-P-04 | 저장소를 public으로 전환하는 작업은 **사용자가 GitHub 웹 설정에서 직접 수행**한다. | 필수 |

---

## 4. 비기능 요구사항

| ID | 요구사항 |
|---|---|
| NFR-01 | 외부 CDN·폰트·라이브러리 의존성 없이 동작한다(오프라인 캐시 및 로딩 속도 확보). |
| NFR-02 | 모든 텍스트 파일(`.md`, `.json`, `.html`, `.css`, `.js`, `.yml`, `.py`)은 UTF-8로 읽고 쓴다. ANSI/CP949 금지. |
| NFR-03 | `access.json`, `.env`, `*token*`, `*secret*` 등 크리덴셜은 절대 커밋되지 않는다. `.gitignore`에 이미 등재되어 있으며 커밋 전 `git check-ignore`로 검증한다. |
| NFR-04 | `CLAUDE.md`, `USERRULE.md` 등 AI 지시문 파일은 커밋하지 않는다. |
| NFR-05 | 페이지 초기 렌더링은 300개 항목 기준 1초 이내에 완료되어야 한다. |
| NFR-06 | 키보드만으로 모든 카드와 필터에 접근·조작할 수 있어야 하며, 포커스 표시가 명확해야 한다. |
| NFR-07 | 본문 텍스트 대비비는 WCAG AA(4.5:1) 이상을 만족한다. |
| NFR-08 | Python 스크립트는 표준 라이브러리 + `feedparser`만 사용한다. |
| NFR-09 | 뉴스 원문은 저장·재게시하지 않으며, 카드에는 항상 매체명을 표기하고 원문으로 연결한다. |

---

## 5. 범위 밖 (Out of Scope)

명시적으로 이번 범위에 포함하지 않는다.

- 뉴스 본문 크롤링 및 전문 저장
- 원문 페이지에서 `og:description`을 긁어와 요약을 만드는 기능 (FR-C-04 참조)
- og:image 등 썸네일 이미지 수집 (텍스트 중심 카드로 확정)
- 사용자 로그인, 북마크, 댓글 등 상태를 서버에 저장하는 기능
- AI 요약·번역·감성분석
- 네이버 검색 API 등 키가 필요한 소스 연동
- 무한 스크롤 / 페이지네이션 (보관 상한 300개로 불필요)
- PWA / 푸시 알림

---

## 6. 제약 및 리스크

| 항목 | 내용 | 대응 |
|---|---|---|
| private 저장소 Pages | private 저장소의 Pages는 GitHub Pro/Team 전용 | 저장소를 public으로 전환 (사용자 직접 수행) |
| 구글뉴스 RSS 링크 | 링크가 `news.google.com` 리디렉트 URL 형태 | 브라우저에서 원문으로 정상 리디렉트되므로 그대로 사용. 향후 원문 URL 해석이 필요해지면 별도 기능으로 검토 |
| RSS 규격 변경 | 구글 측 응답 포맷이 변경될 가능성 | FR-C-12에 따라 실패 시 기존 데이터를 보존하고 워크플로를 실패 처리 |
| Actions 무료 한도 | public 저장소는 Actions 무제한이므로 매시간 실행에 문제 없음 | — |
| 커밋 히스토리 증가 | 매시간 실행으로 커밋이 누적 | FR-D-05(변경 시에만 커밋), FR-D-03(diff 최소화)로 완화 |
| 보관 기간 | 1회 수집 시 중복제거 후 약 500건이 들어와, 상한 300건이면 실질 보관 범위는 최근 약 24시간 | 의도된 동작. 본 서비스는 아카이브가 아니라 속보 카드뉴스로 정의한다. 확대가 필요하면 `MAX_ITEMS` 상수만 조정 |

---

## 7. 완료 기준 (Acceptance Criteria)

1. 로컬에서 `python scripts/fetch_news.py` 실행 시 `data/news_link.json`이 §3.3 스키마대로 생성된다.
2. 5개 키워드 모두에서 기사가 수집되고, 중복 기사가 하나로 병합되어 있다.
3. 스크립트를 두 번 연속 실행해도 기존 항목이 사라지지 않고 누적된다.
4. `index.html`을 로컬 서버로 열면 카드 그리드가 렌더링되고, 카드 클릭 시 새 탭에서 원문이 열린다.
5. 키워드 칩과 검색어를 동시에 적용했을 때 결과가 올바르게 좁혀진다.
6. 모바일 폭(375px)과 데스크톱 폭(1440px) 모두에서 레이아웃이 깨지지 않는다.
7. GitHub Actions를 수동 실행했을 때 `data/news_link.json`이 갱신 커밋된다.
8. 변경이 없는 상태로 재실행하면 커밋이 생성되지 않는다.
9. GitHub Pages URL에서 위 4~6번이 동일하게 동작한다.
10. `git status`에 `access.json`, `CLAUDE.md`, `USERRULE.md`가 나타나지 않는다.
11. 제목이 같고 매체만 다른 기사가 하나로 합쳐지며, 같은 입력에 대해 실행을 반복해도 남는 항목이 바뀌지 않는다.

---

## 8. 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v1.0 | 2026-08-22 | 초기 명세 작성. 수집 소스(구글뉴스 RSS), 키워드 방식, 텍스트 카드, Python, 정적 프론트엔드, 매시간 갱신, public 전환 확정 |
| v1.4 | 2026-08-22 | `generated_at`을 매 실행마다 갱신하면 FR-D-05가 무력화되는 결함을 발견해, 기사 목록이 실제로 바뀐 경우에만 갱신하도록 FR-D-05와 §3.3 스키마 주석을 명확화 |
| v1.3 | 2026-08-22 | 제목 기준 2차 중복제거 추가(FR-C-14, 완료기준 11). 배포 후 실제 화면에서 통신사 기사가 매체만 다른 채로 중복 노출되는 것을 확인해 반영 |
| v1.2 | 2026-08-22 | 디자인 방향 확정. 샌드 옐로우 웜 팔레트와 조밀 레이아웃을 명세화(FR-W-17·18), 테마 수동 전환 버튼과 깜빡임 방지 요구 추가(FR-W-14·15), 결과 건수 표시 추가(FR-W-16), 키워드 색상을 어스 컬러 팔레트로 한정(FR-W-11) |
| v1.1 | 2026-08-22 | Phase 1 실측 결과 반영. 구글뉴스 RSS가 기사 요약을 제공하지 않음이 확인되어 `summary` 필드를 스키마에서 제거하고 제목 중심 카드로 확정(FR-C-02·04, FR-W-02·05, §3.3). 매체명 이중 접미사 제거 규칙 추가(FR-C-03). 보관 상한 300건 유지 근거를 §6에 기록 |
